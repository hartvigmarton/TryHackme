logged in through ftp with default credentials(anonymous)
downloaded 2 files
found username
found password list
bruteforced ssh port with password list
got in
escalated privileges with tar:
sudo tar -cf /dev/null /dev/null --checkpoint=1 --checkpoint-action=exec=/bin/sh
